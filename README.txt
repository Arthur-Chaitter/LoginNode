Olá, se você caiu nesse projeto, eu criei com a intenção de fazer uma api node de login com JWT, isso
usando no node o Express + knex + sqlite3. Agora voltei com algum conhecimento, e vou dar continuidade com
nos proximos dias.

os codigos a baixo servirão para referencias futuras:

npm init -y

npm install express

npm install nodemon -D

npm install knex

npm install sqlite3

npx knex init

npm install jsonwebtoken

npm install @hapi/joi


index > src > app > banco de dados > routes > config > controllers > validations > midllewares.
